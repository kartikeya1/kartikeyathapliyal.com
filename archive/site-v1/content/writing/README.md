# Writing

Future blog posts live here, one `.mdx` file per post.

TODO: when the first post is ready, create `<slug>.mdx` with:

```yaml
---
title: "Post title"
summary: "One sentence."
date: "2026-08-01"
---
```

Posts listed on /thinking replace the "Coming soon" block automatically
(see `getWritingPosts()` in lib/content.ts). This README is ignored by the
content loader.
