# TEMPORARY — phase-3-design branch only.
#
# Turns on the A/B/C design-direction switcher so the Vercel preview for this
# branch is comparable without touching project settings. Contains no secrets.
#
# Deleted in the same commit that collapses the site to the chosen direction.
# This branch must not be merged while this file exists.
NEXT_PUBLIC_DESIGN_PREVIEW=1
